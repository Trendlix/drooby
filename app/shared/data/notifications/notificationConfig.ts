/**
 * Notification Configuration
 * Change the notification count here to update it across the app
 */
export const NOTIFICATION_COUNT = 3;

// You can also make this dynamic by:
// 1. Using a context/state management
// 2. Fetching from an API
// 3. Using localStorage
// Example: export const getNotificationCount = () => { return localStorage.getItem('notificationCount') || 3; }

